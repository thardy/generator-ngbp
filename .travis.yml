language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-ngbp' ]; then cd .. && eval "mv $currentfolder generator-ngbp" && cd generator-ngbp; fi

